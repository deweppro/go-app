version: '3.7'

services:

  app:
    image: markus621/golang:1.0.0
    container_name: deweppro-appgo13
    restart: always
    working_dir: /app
    volumes:
      - ./../../pkg:/app/pkg:ro
      - ./../../go.mod:/app/go.mod:ro
      - ./../../go.sum:/app/go.sum:ro
